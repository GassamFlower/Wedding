import { registerAuth as registerAuthOrigin } from '@cloudbase/auth';
import { ICloudbase } from '@cloudbase/types';

export const registerAuth = registerAuthOrigin as (app: ICloudbase) => void;

try {
  (window as any).registerAuth = registerAuth
} catch (e) {}